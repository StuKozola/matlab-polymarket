name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
    inputs:
      run-integration:
        description: Run live integration tests when repository secrets are configured
        required: false
        default: "false"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: matlab-actions/setup-matlab@v2
        with:
          release: R2024b
      - uses: matlab-actions/run-build@v2
        with:
          tasks: test check package
      - uses: actions/upload-artifact@v4
        with:
          name: Polymarket.mltbx
          path: release/Polymarket.mltbx

  integration:
    if: github.event_name == 'workflow_dispatch' && inputs.run-integration == 'true'
    runs-on: ubuntu-latest
    env:
      POLYMARKET_RUN_INTEGRATION: true
      POLYMARKET_RUN_WEBSOCKET_TESTS: ${{ secrets.POLYMARKET_RUN_WEBSOCKET_TESTS }}
      POLYMARKET_RUN_TRADING_E2E: ${{ secrets.POLYMARKET_RUN_TRADING_E2E }}
      POLYMARKET_EXAMPLE_TOKEN_ID: ${{ secrets.POLYMARKET_EXAMPLE_TOKEN_ID }}
      POLYMARKET_TRADING_TOKEN_ID: ${{ secrets.POLYMARKET_TRADING_TOKEN_ID }}
      POLYMARKET_TRADING_PRICE: ${{ secrets.POLYMARKET_TRADING_PRICE }}
      POLYMARKET_TRADING_SIZE: ${{ secrets.POLYMARKET_TRADING_SIZE }}
      POLYMARKET_TRADING_SIDE: ${{ secrets.POLYMARKET_TRADING_SIDE }}
      POLYMARKET_TRADING_TICK_SIZE: ${{ secrets.POLYMARKET_TRADING_TICK_SIZE }}
      POLYMARKET_TRADING_ORDER_TYPE: ${{ secrets.POLYMARKET_TRADING_ORDER_TYPE }}
      POLYMARKET_TRADING_MAX_NOTIONAL: ${{ secrets.POLYMARKET_TRADING_MAX_NOTIONAL }}
      POLY_ADDRESS: ${{ secrets.POLY_ADDRESS }}
      POLY_API_KEY: ${{ secrets.POLY_API_KEY }}
      POLY_SECRET: ${{ secrets.POLY_SECRET }}
      POLY_PASSPHRASE: ${{ secrets.POLY_PASSPHRASE }}
      POLY_PRIVATE_KEY: ${{ secrets.POLY_PRIVATE_KEY }}
      POLY_SIGNATURE_TYPE: ${{ secrets.POLY_SIGNATURE_TYPE }}
      POLY_FUNDER: ${{ secrets.POLY_FUNDER }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: matlab-actions/setup-matlab@v2
        with:
          release: R2024b
      - name: Install optional Polymarket Python SDK
        run: python -m pip install --upgrade py-clob-client-v2
      - uses: matlab-actions/run-build@v2
        with:
          tasks: java integration
